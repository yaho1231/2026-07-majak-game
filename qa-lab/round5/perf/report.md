# QA 5라운드 Phase D — 서버 «전» 측정표 (2026-09-16)

기준: 브랜치 `claude/qa5-phase-d` (origin/master `ce9d5dd`), 서버 번들 `packages/server/dist/index.mjs`
(11:49 빌드). 머신 8코어, 측정 중 GUI 프로세스가 상시 ~2코어를 먹고 있었다(load avg 5~8) — 절대값은
그만큼 흔들리고, **규모 간 상대 비교**로 읽는다. 최적화는 하지 않았다. 재기만 했다.

원자료: `d1-think0.jsonl` · `d1-think1000.jsonl`(wsLoadCli 행, `perfSeries`에 1초 시계열) ·
`d2-d3-d5-summary.jsonl`(loadCli 요약 + 시계열) · `d6-*.jsonl` · `d7-prof.jsonl` ·
`d7-server-128rooms-think0.cpuprofile`. 로그·힙 스냅샷은 `out/`(gitignore).

## 0. 도구 수정 2건 (packages/server/src, 측정 중 발견)

| 파일 | 무엇 | 왜 |
|---|---|---|
| `loadCli.ts` | `gameOver` 뒤 **`lobby(canStart)`가 오면 startGame 을 다시 보낸다** (`wantRestart`) | 종전엔 gameOver 직후 startGame 을 보냈는데 그때 방은 아직 `playing`(`finishStats` 뒤에야 `resetRoomAfterGame`)이라 **조용히 버려졌다** → 모든 테이블이 첫 판만 돌고 멈춘다(첫 D-2 패스: 정확히 100판, 53초 이후 봇 판단 0). docs/54 의 loadCli 수치도 같은 조건(첫 판만)이었을 가능성이 있다. wsLoadCli 는 원래 lobby 에서 재시작해 영향 없음 |
| `wsLoadCli.ts` | jsonl 행에 `perfSeries`(샘플별 loop p99/max·bot p99·heap·rss) 추가 | 스파이크 **시각** 상관(D-5·D-6) — 표 출력은 그대로 |

## 1. D-1 구간표 — wsLoadCli, realPlayers=1, 60초, 진짜 ws + deflate

### think 0 (봇·자동 0ms = 최대 속도, CPU 포화 구간)
| 방 | 소켓 | ping p50/p95/p99 ms | action p50/p99 | loop p99/max | bot p99/max | heap/rss MB | 거절 | INVALID | 프레임/s | 판/국(60초) |
|---|---|---|---|---|---|---|---|---|---|---|
| 16 | 16 | 32/83/117 | 54/156 | 253/253 | 20/101 | 143/269 | 0 | 3 | 1280 | 139/794 |
| 32 | 32 | 55/176/258 | 92/256 | 329/329 | 10/61 | 192/341 | 0 | 5 | 1246 | 128/767 |
| 64 | 64 | 76/322/438 | 101/305 | 390/390 | 5/68 | 245/435 | 0 | 11 | 1357 | 120/781 |
| 128 | 128 | 333/662/957 | 145/561 | 718/718 | 5/58 | 288/553 | 0 | 8 | 1497 | 108/811 |
| 200 | 200 | 579/1220/1678 | 259/606 | 460/460 | 10/46 | 363/615 | 0 | 7 | 1410 | 28/691 |

- think 0 은 «서버 한 대의 처리량 상한»이다: 방을 16→200 으로 늘려도 **국/60초 ≈ 700~800, 프레임/s ≈ 1,300~1,500 로 일정** — 단일 스레드가 포화돼 방이 늘수록 방당 진행이 느려질 뿐이다. loop p50 이 500~800ms 로 뜨는 것은 «정지»가 아니라 «항상 바쁨». 이 구간의 ping 은 큐 대기라 렉 순위에 못 쓴다.
- 단독 예비 실행(16방, 표 밖)에서 loop max **1,052ms** 가 한 번 나왔다 — 재실행에선 253. 노이즈(GUI)와 구분 못 함.

### think 1000 (BOT_THINK_MS=AUTO_MOVE_MS=1000 = 실전 속도)
| 방 | 소켓 | ping p50/p95/p99 ms | action p50/p99 | loop p99/max | bot p99/max | heap/rss MB | 거절 | INVALID | 프레임/s | 국(60초) |
|---|---|---|---|---|---|---|---|---|---|---|
| 16 | 16 | 1/6/13 | 3/41 | 21/28 | 50/22 | 62/205 | 0 | 0 | 52 | 13 |
| 32 | 32 | 1/10/17 | 3/59 | 42/115 | 20/17 | 50/223 | 0 | 0 | 107 | 30 |
| 64 | 64 | 2/8/11 | 3/122 | 104/139 | 100/73 | 52/242 | 0 | 0 | 209 | 49 |
| 128 | 128 | 4/8/10 | 2/159 | 104/160 | 20/55 | 161/374 | 0 | 0 | 403 | 113 |
| 200 | 200 | 5/14/78 | 2/125 | 33/81 | 10/39 | 167/403 | 0 | 0 | 623 | 179 |

- **200방·200소켓(상한 MAX_ROOMS=200)까지 거절 0·INVALID 0**, ping p99 ≤ 78ms, loop max ≤ 160ms. 실전 속도에서는 상한까지 «렉 없음»이 수치로 나온다.
- loop max 의 최댓값은 **모두 측정 창 1초째**(방 준비 직후 = 100~200판이 동시에 시작하는 순간, 아래 §2 «동시 시작 버스트»). 그 뒤 60초 동안은 ≤ 60ms 산발.
- `bot p99` 열은 perfMonitor 버킷(2/5/10/20/50/100ms) 상한이라 max 보다 클 수 있다(64방 «100/73»). 실제 봇 판단은 평균 0.5~1.1ms, max ≤ 100ms.
- 300연결(realPlayers=4)·상한 상향 실험(D-10)은 하지 않았다(시간).

## 2. D-2 조건 변형 — loadCli 100테이블 60초 (가짜 소켓, 같은 프로세스)

### think 1000 (실전 속도 — loop 값이 의미 있는 쪽)
| 조건 | CPU %코어 | loop p50/p99/max | 1초째 제외 spikes>30ms | bot mean/p99/max | heap/rss MB |
|---|---|---|---|---|---|
| hard(기본) | 43 | 2/235/235 | 103,77,63,39,42,50,59,81 (8개) | 0.77/50/47 | 101/345 |
| normal | 45 | 2/259/259 | 41,36,42,39 (4개) | 1.08/50/50 | 103/272 |
| easy | 45 | 2/276/276 | 36,45,75,68,60,72,75,102,39,45 (10개) | 1.11/20/76 | 106/336 |
| --augments | 43 | 2/274/274 | 33,90,46,45 (4개) | 0.79/50/43 | 101/171 |
| --spectate 2 | 43 | 2/24/85 | 32,72,30,85,33 | 0.77/10/84 | 102/337 |
| **--friends 10** (1,000쌍) | 45 | 2/314/314 | **30,56,55,89,31,77,106,102,97,79,33,99,160,120,110,114,134,89,253,91,31,267,44,33,39 (25개, 100~267ms 반복)** | 0.76/20/93 | 100/442 |
| --seed-users 2000 | 43 | 2/260/260 | 54,35,40,54,33,140,259 | 0.75/50/50 | 119/293 |

- 모든 조건의 p99/max ≈ 235~314ms 는 **1초째 «100판 동시 시작» 한 번**이다(spectate2 만 준비가 길어 시작이 창 밖으로 나가 24/85).
- **friends 10 만 창 내내 100~267ms 스파이크가 반복된다** — 접속·이탈 없이도 프레즌스 경로(S-4)가 계속 도는 것으로 보인다(정확한 트리거는 미확인: 국 전환·통계 브로드캐스트 후보).
- 난이도·증강 전량·관전 2석은 CPU·bot·loop 에서 hard 와 구분되지 않는다.

### think 0 (최대 속도 — CPU·메모리 비교용, loop 는 포화라 무의미)
| 조건 | CPU %코어 | 끝난 판/60초 | loop p50/p99 | bot mean/p99/max | heap/rss MB (창 최대) |
|---|---|---|---|---|---|
| hard | 121 | 138 | 483/726 | 0.53/5/50 | 149/724 |
| normal | 119 | 97 | 621/802 | 0.80/10/96 | 150/654 |
| easy | 119 | 93 | 657/788 | 0.78/10/156 | 149/681 |
| --augments | 119 | 104 | 828/864 | 0.53/20/91 | 144/642 |
| --spectate 2 | 119 | 132 | 498/**1786** | 0.52/5/131 | 156/740 |
| --friends 10 | 119 | 124 | 538/792 | 0.55/50/553 | 145/675 |
| **--seed-users 2000** | 121 | 115 | 741/800 | 0.53/10/143 | **2,179 / 3,019** |

- **seed-users 2000 = 힙 2.2GB·rss 3.0GB** (다른 조건 150/700). 시계열: 판이 끝나기 시작하는 30초부터 힙이 직선으로 오른다(34s 855 → 52s 2,431MB). 판 종료마다 `StatsStore.record → save` 가 2,100명 문서 전체를 `JSON.stringify(…, null, 2)`(운영 stats.json 기준 ≈10KB/명 → ≈21MB/회) 하고, 그 스냅샷 문자열을 `saveChain` 이 순서대로 쓰는 동안 붙들고 있다 → 60초에 115판 종료 = 2.4GB 문자열 생성, 쓰기가 밀리면 누적. **S-5 확정**(현 운영 91명·0.9MB 에서는 작지만 유저 수 × 판 종료율에 비례).
- spectate2 의 1,786ms 단발(28초째): 관전 뷰 원가(X-8)가 의심되나 1회라 확정 못 함.
- think 0 은 어느 조건이든 **≈1.2코어에서 포화**(단일 스레드 + GC/IO 약간). 판/60초가 조건마다 93~138 로 다른 것은 판 길이 차이(난이도·증강)이지 원가 차이가 아니다.

## 3. D-3 소크 — loadCli 200테이블, 실전 속도, 120초, 힙 스냅샷 3장

| 구간 | loop p99 / max | heap(샘플) | rss |
|---|---|---|---|
| 5s | 168 / 1,167 (동시 시작) | 120 | 342 |
| 10~55s | 21~30 / 31~79 | 126 → 202 (GC 전 톱니) | 347 → 438 |
| 65s | 35 / **4,772** ← `writeHeapSnapshot`(동기, 도구 원가) | 175 | 988(스냅샷) |
| 70~120s | 38~79 / 54~249 | 214~658 톱니 | 1,565 |
| 끝(gc 후) | — | **182** | 1,563 |

- CPU 172s/120s = 143%코어 — 스냅샷 2장(각 ~10초 CPU)이 섞였다. 실전 속도 순수 원가는 D-2 기준 테이블당 0.4~0.5%코어 → 200테이블 ≈ 0.9코어.
- 힙 스냅샷 인스턴스 수(self 합, ready 40MB → mid 174MB → shutdown 173MB):

| 클래스 | ready | mid(60s) | shutdown+gc |
|---|---|---|---|
| HumanAgent | 200 | 200 | **176** |
| BotAgent | 601 | 601 | 529 |
| GameEngine / HanchanController / EffectRegistry | 0 | 200 | **176** |
| ReplayWriter | 200 | 200 | 176 |

- **shutdown 뒤 176/200 판이 남는 이유**(리테이너 경로 추적): `global.setTimeout → timerListMap[180000] → Timeout._onTimeout → HanchanController.game → engine`. 즉 `shutdown()` 의 `detachRoomConns` 가 사람 좌석을 `suspend(180_000)`(SOLO_HOLD_MS / 대기 결정 재무장)로 세워 **3분 타이머가 컨트롤러·엔진을 붙든다**. 운영에선 shutdown 직후 프로세스가 끝나므로 누수가 아니고, 테스트·loadCli·«방 닫기 없이 접속 끊김» 경로에서만 3분간 173MB 가 남는다. 2분 소크에서 **무한 성장 징후는 없음**(S-9 는 30분 소크 미실시 → 미확정). 1분 진행 판 하나가 붙드는 힙 ≈ 0.67MB(134MB/200).
- 스냅샷 크기: ready 31MB · mid 247MB · shutdown 249MB.

## 4. D-5 sweep 버스트 — loadCli 200테이블, `ROOM_SWEEP_INTERVAL_MS=5000`(env 로 읽힘, RoomManager.ts:774, 최소 1000), 실전 속도, `--stmt-count`

| 초 | 1 | 4 | 9 | 14 | 19 | 24 | 29 | 34 | 39 | 44 | 49 | 54 | 59 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| stmt +/s | 1,801(준비) | **+201** | +200 | +201 | +201 | +202 | +203 | +202 | +206 | +203 | +203 | +203 | +209 |
| loop max ms | 523(시작) | 26 | 28 | 12 | 32 | 14 | 7 | 20 | 30 | 28 | 36 | 12 | 90 |
| (비sweep 초의 loop max) | | 18~157 | 27~177 | 15~44 | 7~52 | 10~58 | 8~16 | 7~24 | 17~34 | 8~28 | 23~30 | 26~68 | 4~47 |

- sweep 마다 `INSERT INTO live_games …`(upsert) **정확히 방 수(200)회**. 60초 누계 2,769회 · 196.5ms → **회당 0.07ms, sweep 1회 ≈ 14ms**. sweep 초의 loop max 는 비sweep 초와 구분되지 않는다.
- WAL + `synchronous` 기본(FULL)인데도 이 정도다(임시 디렉터리, APFS). **S-1 은 이 부하에서 루프 정지를 만들지 않았다.**

## 5. D-6 스레드풀 경합 — 서버 직접 기동(dist, think 1000) + wsLoadCli `--attach` 100방 + 별도 스크립트로 **register(scrypt) 20건/초 × 50초**

| 실행 | UV_THREADPOOL_SIZE | ping p50/p95/p99 | action p50/p95/p99 | loop p99/max | authOk p50/p95/p99/max | 거절/실패 |
|---|---|---|---|---|---|---|
| tp4 (1회차) | 4 | 38/122/**849** | 3/167/170 | **366**/366 | 124/232/780/1025 | 0/0 (980건) |
| tp4b (2회차) | 4 | 2/9/31 | 3/90/160 | 132/201 | 131/228/253/299 | 0/0 |
| tp16 | 16 | 3/8/**11** | 2/13/33 | **34**/147 | 132/225/237/254 | 0/0 |
| (대조: D-1 128방, 로그인 없음) | 4 | 4/8/10 | 2/–/159 | 104/160 | – | – |

- 로그인 폭주가 있으면 ping p99 가 10 → 31~849, loop p99 가 104 → 132~366 으로 는다. 스레드풀 16이면 로그인 없는 대조군 수준(11/34)으로 돌아온다 — **S-6(스레드풀 4 공유) 방향의 증거**. 다만 tp4 두 회차 차이(849 vs 31)가 크다 → 노이즈(GUI 2코어) 포함, **각 1~2회 측정이라 확정 아님**. tp4 1회차의 366ms 는 56·60초째(폭주 끝 무렵, 서버 기동 60초 = 첫 sweep 과 겹칠 수 있음).
- 인증 1건 자체의 왕복 p50 ≈ 125~130ms(scrypt) 는 풀 크기와 무관 — 큐 대기(p99)만 준다.
- S-3(인증 1건 = 카탈로그·스캔·클라 후속 쿼리 55건)은 이 실험에서 분리되지 않는다(폭주 클라는 authOk 만 받고 끊는다).

## 6. D-7 프로파일 — wsLoadCli 128방 think 0, 자식 서버에 `NODE_OPTIONS=--cpu-prof` (61.6초, 42,584 샘플)

self-time 상위 15:
| % | 함수 | 위치 |
|---|---|---|
| 6.8 | (garbage collector) | |
| 6.2 | isStandardWinningShape | core 손패 판정 |
| 5.6 | combineGroups | core 분해 |
| 5.3 | winningKindsOf | core |
| 3.6 | buildPlayerView | core 뷰 |
| 3.3 | shantenFast | core |
| 2.9 | canExtractSets | core |
| 2.5 | (idle) | |
| 2.3 | buildCounts | core |
| 2.1 | sortForDisplay | core 뷰 |
| 2.0 | profilesOf | core shanten |
| 2.0 | takeGroup | core |
| 2.0 | guessYaku | server 인사이트 |
| 1.9 | push | core |
| 1.9 | sendView | server HumanAgent |

inclusive: `decide`(봇) **28.6%** · `validate/validateReason`(액션 검증 → winningKinds) **23%** · `buildPlayerView` **19.9%** (그중 shantenOf 14.2·ukeireOf 13.7·guessYaku 3.9) · `broadcastViews` 22.9% · `sendView`(stringify+ws.send) **2.6%** · zlib/deflate self **0.5%** · `saveLiveGame` 0.4% · `EffectRegistry.matching` **0.1%** · StatsStore save/recordGame ≈ 0 · ws sender/receiver/permessage-deflate 각 0.1%.

- CPU 는 **엔진(검증·손패 판정·샹텐)과 뷰 구성이 70%+**, 서버 계층(직렬화·압축·DB·통계·증강 매칭)은 합쳐 **< 5%**. S-2·S-5·S-8 의 CPU 가설은 이 규모에서 기각.
- `validate` 23%: 액션 검증마다 `winningKindsOf` 가 도는 구조 — docs/54 의 «엔진 4배 절감» 뒤에도 가장 큰 self 덩어리다(최적화 후보로 새로 적어 둘 것, 코어 골든으로 불변 증명 가능).

## 7. D-9 폴백 빈도

- **폴백 카운터가 없다**(C-4 미구현 — `perfMonitor.ts` 에 `safeDecide` 폴백·`time_pressure` 만료 카운터 없음). 대신 wsLoadCli 의 INVALID_ACTION·거절을 모든 실행에 표기했다.
- think 1000: 모든 규모에서 **INVALID 0·거절 0**. think 0: 3~11건/60초 — 전부 `이미 끝난 선택입니다`(promptCancel 뒤 늦게 도착한 답, 클라 즉답 경쟁) → 서버 폴백이 아니라 부하 클라의 레이스. `time_pressure`(5초) 만료 여부는 잴 수단이 없었다 — ping p99 ≤ 78ms(실전 속도 200방)라 5초 초과는 없었을 것으로 본다.

## 8. 미측정
- **D-4 재시작 폭주**(유저 300×50판 시드 + live_games 100 + 동시 tokenLogin→replayList…): 시드·시나리오 도구 없음, 시간.
- **D-8 부팅 이어하기 폭주**(X-3): 종반 리플레이 fixture 준비 필요, 시간.
- **D-10 상한 상향**(300방/400연결 env): 시간. 200방/200소켓까지는 거절 0.
- 30분 소크(S-9): 120초까지만.
- 관리자 폴링(`liveGames`·`listUsers`) 부하(X-8): 미측정.

## 9. 최적화 후보 순위 — «측정에서 실제로 루프 정지·지연을 만들었는가»

| 순위 | 후보 | 판정 | 근거 수치 |
|---|---|---|---|
| 1 | **S-5 stats.json 전량 stringify** | **만들었다 (메모리)** — 유저 수에 비례해 폭발 | seed 2000 × think 0: 힙 2,179MB·rss 3,019MB (기본 150/700), 판 종료 시작과 함께 직선 상승. 현 운영 91명·0.9MB 는 안전, «수백 명»부터 위험. CPU 프로파일에는 안 잡힘(91명 기준) |
| 2 | **S-6 스레드풀 4 공유 (+S-3 인증 경로)** | **만들었다 (지연, 노이즈 큼)** | 로그인 20/s 폭주: ping p99 10 → 31~849, loop p99 104 → 132~366; `UV_THREADPOOL_SIZE=16` → 11/34. 1~2회 측정, 재확인 필요 |
| 3 | **S-4 친구 프레즌스** | **만들었다 (반복 스파이크)** | friends 10(1,000쌍) 실전 속도: 60초 동안 100~267ms 스파이크 25회(기본 8회·≤103ms); p99 314 vs 235 |
| 4 | **(신규) 동시 시작 버스트** — 100~200판이 같은 틱에 시작 | 만들었다 (단발) | 모든 실전 속도 실행의 1초째 loop max 235~523ms(D-2·D-5), 1,167(D-3 200판). 재시작 이어하기(X-3·D-8)와 같은 모양 → E 후보로 «시작 분산» 검토 |
| 5 | **(신규) 액션 검증 → winningKinds** | CPU 최대 덩어리 (정지 아님) | 프로파일 inclusive 23%, self 상위 3개(isStandardWinningShape·combineGroups·winningKindsOf 17%) |
| 6 | S-2 좌석별 stringify + deflate | **안 만들었다** | sendView 2.6% + zlib 0.5%; 200방 실전 ping p99 78ms; rss 200소켓 403MB |
| 7 | S-7 측정 조건 공백(normal/easy·증강·관전) | 조건 채웠고 **차이 없음** | CPU 119~121%(think 0)·43~45%(think 1000) 전 조건 동일; bot max ≤ 156ms. spectate2 의 1,786ms 단발만 미해명 |
| 8 | S-9 메모리(eventLog·deflate·profileCache) | 2분 내 **징후 없음** (30분 미실시) | 1분 판당 ≈0.67MB, gc 후 182MB/200판. shutdown 잔존 176판은 180초 hold 타이머(테스트 전용 현상) |
| 9 | S-1 sweep 동기 upsert | **안 만들었다** | 200방 sweep = 200 upsert ≈ 14ms(회당 0.07ms), sweep 초 loop max 7~36ms = 비sweep 초와 동일 |
| 10 | S-8 EffectRegistry.matching | **안 만들었다** | inclusive 0.1% |

S-3 단독(카탈로그 직렬화·replayList 55쿼리·리더보드)은 D-4 미측정이라 «미확정». X-1 상한(200방/300연결)은 실전 속도에서 성능 여유가 있다(200방 loop max 81ms) — 상향은 사용자 판단으로 §8 에.
