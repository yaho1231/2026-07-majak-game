# QA 4라운드 수정 로그 — client-fix-B (2차 배치)

담당 파일: `packages/client/src` + `packages/client/test`.
서버는 **`HumanAgent.ts` 한 파일만** 예외로 손댔다(온보딩 P1 ①의 원인이 그 파일의 상수·분기다).
읽은 보고서: `qa-lab/round4/onboard.md` · `loop.md` · `text.md`.
1차 배치(`fix-client-a.md`)가 넣은 것(`PickTimer`·`InfoNote`, 파일 끝 CSS 4블록, 새 prop 2개)은 **그대로 살아 있다** — `qaRound4ClientA.test.ts` 25건 전부 통과.

검증:
- `npm run typecheck:client` · `npm run typecheck:server` · `npm run typecheck:content` · `npm run typecheck` — **에러 0**
- `npx vitest run packages/client` — **44 파일 · 713 테스트 전부 통과** (신규 54건 포함)
- `npx vitest run packages/server/test/Tutorial.test.ts packages/server/test/HumanAgent.test.ts` — 69건 통과
- 회귀 테스트: `packages/client/test/qaRound4ClientB.test.ts` (신규, 54건)

---

## 고친 것 — onboard P1

| 항목 | 고친 방법 | 근거/테스트 |
|---|---|---|
| **P1 ①** 「바로 한 판」(게스트)·연습 대국에 실전과 같은 30초 드래프트 제한 | `HumanAgent`에 `FIRST_DRAFT_TIMEOUT_MS`(**75초**)를 새로 두고, `draftTimeoutMs()`가 «이 판에서 몇 번째 드래프트인가»(`draftsOffered`)로 가른다. 첫 스테이지만 넉넉하고 두 번째부터는 평소대로 30초다. **방이 게스트/연습인지는 `HumanAgent`가 모른다**(그건 `RoomManager`가 쥔 사실이고 나는 그 파일을 못 만진다) — 대신 누구에게나 참인 사실로 갈랐다: 카드 셋을 한 번도 읽어 본 적이 없는 순간은 «대국 개시» 드래프트 하나뿐이다. 네 좌석 전부에 같은 값이 걸리므로 유불리가 없고, 판은 한 번 45초 더 기다릴 뿐이다. | 테스트 4건. **보고서가 예로 든 90초를 그대로 쓰지 않았다** — `HanchanController.AGENT_DECIDE_TIMEOUT_MS`(90초, 드래프트 경로에도 걸린다)의 전제가 «정상 흐름에서는 좌석 자신의 타이머가 항상 먼저 터진다»라, 90으로 맞추면 그물이 사람을 대신 골라 버린다. 테스트가 `30_000 < 값 < 90_000`을 못 박는다. |
| **P1 ②** 튜토리얼이 실전 30초 제한을 한 번도 안 가르친다 | `codex` 강의의 caveat 형태 그대로, **`outro`** 본문에 한 줄. "실제 대국에는 한 수마다 30초가 걸립니다 — 시간이 다 되면 자동으로 패스하거나 방금 가져온 패가 그대로 나갑니다(증강 고르기는 첫 번째만 넉넉합니다)." 나가기 직전이 그 말을 들을 자리다. 덤으로 랜딩 안내문도 갈랐다 — 예전엔 "시간 제한이 없습니다"가 두 문 **모두**에 걸리는 것처럼 읽혔다. | 테스트 2건(«30초» + 결과까지 말하는가). |
| **P1 ③** 첫 화료 정산 화면에서 코치가 통째로 막힘 + 역·부 용어 링크 없음 | 둘 다 했다. ⓐ 정산 화면 전용 강의 `result-score` 신설(«판 × 부» 풀이). `allowedNow`를 `RESULT_LESSONS`(=`result-score`·`outro`) 화이트리스트로 바꿨다 — 통째로 열지 않은 이유는 원래 막아 둔 이유가 옳기 때문이다(판의 사실만 보는 강의가 정산 화면 위에 "이제 기다립니다"로 떴다). 마무리보다 **앞에** 뒀다: 둘 다 성립하는 순간이 오는데 뒤에 있으면 판을 접은 뒤에 뜬다. ⓑ 「N판 M부」 원만 `TermText`로 흘린다 — 역 이름 줄은 그대로 뒀다(원 주석의 이유가 거기엔 그대로 걸린다). 「부」가 안 잡혀서 `fu` 항목에 `\d+부(?![수장])`를 더했다(`han`의 `\d+판`과 같은 방식). | 테스트 5건. 「일부·전부」 같은 일반어를 안 잡는지까지 검사한다. |
| **P1 ④** 뒤로가기·새로고침 방어 0건 | `beforeunload` + `pushState`/`popstate` 두 갈래. 판·대기실에 있을 때만 걸고 관전자는 뺀다(잃을 판이 없다). 되묻는 창은 **`askConfirm`** 이다 — `window.confirm`은 메인 스레드를 멈추는데 서버 결정 타이머는 흐른다(감사 5-9, 테스트가 저장소 전역에서 막고 있다). 비동기라 되묻는 동안 뒤로가기가 또 눌려도 새지 않게 **먼저** 완충 항목을 다시 쌓고 묻는다. 게스트에게는 다른 문장을 준다. | 테스트 4건. |

**게스트 복귀 수단은 이미 있었다** — 보고서의 "게스트는 복귀 수단이 아예 없다"는 절반만 맞다. `GUEST_TOKEN_KEY` → `guestResume` 경로가 살아 있어 **새로고침·재방문으로는 그 판에 다시 붙는다**(App.tsx:3569). 없던 것은 그 사실을 알려 주는 것뿐이라, 새로 만드는 대신 되묻는 창의 문장에 실었다.

## 고친 것 — loop P1

| 항목 | 고친 방법 | 근거/테스트 |
|---|---|---|
| **P1 ①** 초대 + 「계정 없이 시작」 → 초대 증발 | 보고서의 제안 (a). `inviteNeedsAccount()` — 초대 코드가 있으면 게스트 두 문(튜토리얼·바로 한 판)이 **둘 다** 그 자리에서 멈추고 가입 탭으로 보내며, 거절 사유를 그 탭에 남긴다. 초대 배너에도 «초대받은 방에는 **계정으로만** 들어갈 수 있습니다»를 붙였다. `authOk`의 게스트 조기 반환은 건드리지 않았다 — 서버가 게스트에게 `joinRoom`을 안 여니(화이트리스트) 그 분기를 고쳐도 살릴 방법이 없다. **누르기 전에** 막는 것이 유일한 답이다. | 테스트 3건. 「두 버튼 **모두**에 걸려 있는가」를 개수로 못 박았다 — 한쪽만 막으면 그대로 샌다. |
| **P1 ②** 시작된 방에 늦게 온 손님이 갈 곳 없음 | 서버 일(관전·대기 줄)은 못 한다. 클라이언트가 할 수 있는 것만 했다: `ROOM_PLAYING`으로 거절당하면 그 코드를 붙들어 홈의 참가 칸에 **채워 두고**, 토스트가 사라져도 남는 안내 줄(`.home-join-busy`)로 «끝나면 «참가»를 다시 눌러 보세요 — 반장전이면 30~40분»을 적는다. 재시도가 6자리 재입력이 되지 않는다. 들어가는 데 성공하면 놓는다. | 테스트 3건. |
| **P1 ③** 연습 대국 결과 화면에 「한 판 더」 없음 | `GameOverModal`에 `onPracticeAgain`을 새로 달았다 — 누르면 홈으로 돌아가 `practicePlay`를 다시 보낸다(방을 유지할 필요가 없다, 게스트 경로가 이미 그렇게 한다). «방금 그 판이 연습이었나»는 서버 응답에 없는 사실이라 **보낸 쪽에서** 기억한다(`wasPractice`) — `practicePlay`를 보낼 때 켜고, 사람 방에 들어가거나 방을 만들 때 끈다. | 테스트 3건. **「다시 보기」는 못 붙였다** — 서버가 게스트 방에 `recordedId`를 주지 않아 `gameId` 자체가 없다(`RoomManager.ts:5893`). 클라이언트에 만들어 낼 수 있는 값이 아니다. |

## 고친 것 — loop P2 (값이 큰 것)

| 항목 | 고친 방법 | 근거/테스트 |
|---|---|---|
| 빈 전적 CTA 모순 | «튜토리얼로 한 판» 옆에 «방 만들기 (전적에 남음)»을 세우고, 안내에 «튜토리얼과 연습 대국은 기록에 남지 않습니다 — 익힌 뒤 방을 만들어 보세요»를 잇는다. 시키는 대로 한 판 두고 돌아와도 화면이 안 바뀌던 길을 끊는다. | 테스트 1건. |
| 결과 화면에 증강 성적 없음 | `GameAugmentRow` 신설 — 통계 탭의 «이번 판» 그리드 아래에 그 판에 고른 증강 이름을 계열 색으로 세운다. **새 데이터도 새 메시지도 없다**(`StatsEntry.stats.augments`가 이미 온다). 누적용 `PersonalAugmentStats`를 그대로 쓰지 않은 이유: 그쪽의 «시그니처»·«함정»은 표본이 여러 판이어야 뜻이 서고, 한 판에서는 전부 1과 0이라 헛말이 된다. | 테스트 2건. |
| 대기실 전적 칩이 표본을 숨김 | 10국 미만이면 %를 **아예 접고** «전적 N국 — 아직 적음»만 보인다. 10국 이상이면 비율 앞에 «N판 · M국» 표본 칩이 **맨 앞에** 선다. | 테스트 2건(칩이 화료율보다 앞인지 위치로 검사). |
| 공유 링크 내리는 UI 없음 | 뷰어에 «🔗✕» 버튼 — `{ type: "replayShare", gameId, revoke: true }`. 서버·프로토콜·토스트가 전부 준비돼 있었고 **요청만 아무도 안 보냈다**(`grep revoke` 0건). `replayData`에 토큰 여부가 안 실려 오므로 «이 세션에서 만든 판»에만 뜬다(`sharedGames`) — 방금 만든 링크를 그 자리에서 내리는 것이 이 버튼의 용도라 그거면 충분하다. | 테스트 3건. |
| 새로 고침 버튼 4개의 중복 요청 | 쿨다운을 **모듈 전역 하나**로 모으고 900ms → **2,500ms**. 네 버튼이 같은 `refreshHome`을 부르므로 쿨다운도 하나여야 한다. 홈 진입(`refreshHome`)도 같은 창을 태운다 — 판을 끝내고 들어와 두어 번 누르면 걸리던 길이 그거였다. 10초 창에 최대 4회라 서버 한도(5회) 안쪽이다. | 테스트 3건. |

## 고친 것 — text P2 **9건 전부**

| # | 고친 방법 |
|---|---|
| ① 오카·반환점 풀이가 실제 정산과 다름 | **문구를 실제 정산에 맞췄다**(지침대로 코드가 아니라 문구를 고쳤다). 확인한 사실: `DEFAULT_HANCHAN_CONFIG.oka = 0`, 서버에 덮어쓰는 곳 0건, 성적식은 `raw − startScore(25000) + uma×1000`, `returnScore`는 `HanchanController:1885` 서든데스 판정에만 쓰인다. → 「오카」는 «이 서버에서는 0이라 실제로는 오가지 않는다»로, 「반환점」은 «서든데스 판정 기준점»으로, 「우마」 긴 풀이에 실제 기준선 25,000을 명시. 파일 머리말과 §종국 정산 주석의 낡은 «오카 +20» 예시도 걷었다. 항목은 **내리지 않았다** — 다른 마작에서 배워 온 사람이 20,000점을 계산에 넣지 않게 하는 것이 이 항목의 새 용도다. |
| ② 「판」 의미 뒤집힘 | 도움말 섹션 제목 «판은 언제 끝나는가» → **«대국은 언제 끝나는가»**. 튜토리얼 `zoom` 강의의 «판이 안 맞으면/판이 잘리거나» → «화면». (han 뜻의 «판»과 `11125`의 «판수»는 그대로 둔다 — 게임 단위를 «대국»으로 모으는 것이 보고서의 제안이고 «판수»는 이미 굳은 지표 이름이다.) |
| ③ 액티브 배지 ⚡/✦ | **✦로 통일**(실제 버튼이 ✦라 손실이 적다) — 이름표 배지·팁·`aug-tip-active`·드래프트 노트 넷. 겹치던 계열 아이콘은 «기타» 쪽을 `✦ → 🔮`로 옮겼다(«리치»의 ⚡는 그대로 — 통일 글리프가 더는 ⚡가 아니다). 도움말 배지 범례에 «✦ 액티브 는 직접 눌러 발동하는 증강» 한 줄 추가. `act-riichi-aug` 버튼의 ⚡ **둘은 남겼다** — 그건 액티브 배지가 아니라 리치 계열 표식이고, 같은 줄의 «리치» 버튼과 짝이다. |
| ④ `call` 표시 이름 | 용어 분류 라벨 «울기·깡» → **«후로(울기)·깡»**. 증강 계열 필터가 쓰는 «후로»로 모으고 병기는 여기 한 번만(보고서의 «가장 싼» 안). |
| ⑤ 「펑」 | «다른 자리의 선언(론·**치·퐁**·깡)을 기다리는 중입니다» — 표기와 나열 순서를 동시에 맞췄다. 남은 「펑」은 주석뿐이다. |
| ⑥ 「야쿠」 | «(점수·**역**을 보지 않습니다)». 사용자 문자열에서 「야쿠」 0건. |
| ⑦ 화면 크기 단축키 Ctrl→Alt | «… +/− 버튼이나 **Alt(⌥) + − / +** 로 맞춰 보세요. 그래도 좁으면 브라우저 확대 Ctrl(⌘) + − 를 씁니다.» (`uiScale.ts:405`가 ctrl·meta면 손을 뗀다 — 둘은 별개 장치다.) |
| ⑧ 되묻는 창 `\n` | `.confirm-body { white-space: pre-line; }`. |
| ⑨ 천단위 콤마 | 보고서 표의 전수를 고쳤다 — App.tsx 8곳(서든데스 배너·로비 모드 설명 둘·HUD 툴팁·모드 노트·리치 1,000점 둘), glossary.ts 8곳(3000/1000×3/8000/12000/16000/32000), tutorial.ts 1곳. **곁가지 하나**: 사전 본문에 콤마가 붙으면서 용어 검색의 «1000점»이 «1,000점»을 못 찾게 됐다(placeholder가 그 약속을 적고 있다) — 검색어와 본문 양쪽에서 콤마를 걷어 같은 것으로 본다. |

테스트: text 9건에 **17건**. ⑨는 사전 본문 전체를 정규식으로 훑어 «콤마 없는 4자리 점수»가 하나라도 남으면 실패한다.

---

## 고치지 않은 것 (이유를 적는다)

- **onboard P2 ⑥ «랜딩에 규칙 버튼»** — 지침대로 **죽은 분기만** 걷었다. `backLabel={auth === null ? "← 로그인으로" : "← 닫기"}` → `backLabel="← 닫기"`. 규칙 진입점을 되살리는 것은 2026-08-19 사용자 지시(docs/35 §2, App.tsx:7326-7330 "처음 온 사람이 이 화면에서 할 일은 하나뿐이다: 들어가는 것")를 뒤집는 일이라 **판정 대기**로 남긴다. 테스트가 «`AuthScreen`에 `onOpenHelp`를 새로 만들지 않았는가»까지 못 박아, 다음 사람이 무심코 되살리면 걸린다.
- **loop P1 ③의 «다시 보기»** — 서버가 게스트 방에 `recordedId`를 안 준다(위 표). 서버 담당 몫.
- **loop P2 ⑧ 리플레이 OG 카드 · ⑨ 방 30분 유휴 · ⑪ 빈 로비 접속자 수** — 전부 서버·프로토콜 파일이다(`ogMeta.ts`·`index.ts`·`RoomManager.ts`·`protocol.ts`). 담당 밖.
- **onboard P2 ⑤·⑦·⑧·⑨·⑩, P3 전부 / loop P3 전부 / text P3 전부** — 지침 3(P0→P1→P2, P3는 건너뛴다)과 브리프의 담당 범위(loop는 «값이 큰 것», text는 «P2 9건»)를 따랐다.
- **text 의심 1건**(「0이면 날 수 없습니다」) — 의심은 손대지 않는다.

## 남의 테스트를 건드린 곳 (셋, 전부 «옛 동작을 못 박고 있던» 것)

1. `packages/server/test/Tutorial.test.ts` — «체험판은 종전대로 30초 마감이 실린다»가 **onboard P1 ①이 지목한 바로 그 동작**이었다. 새 동작(첫 드래프트만 넉넉)으로 바꾸고, «그 값이 컨트롤러의 90초 그물보다 작은가»를 검사하는 테스트를 하나 더 얹었다.
2. `packages/server/test/HumanAgent.test.ts:571` — 자동 선택을 기다리는 `advanceTimersByTime`이 30초였다. 첫 드래프트라 `FIRST_DRAFT_TIMEOUT_MS`로 바꿨다(검사 내용은 그대로).
3. `packages/client/test/onboardingBatchI.test.ts:164` — «빈 카드에 누를 것이 있다»가 320자 창으로 슬라이스했는데 안내 한 줄이 늘어 창 밖으로 밀렸다. 창을 640자로 넓히고, **«기록에 남는 문(`onCreateRoom`)도 함께 서 있는가»를 한 줄 더 못 박았다** — 이 테스트가 지키려는 것은 «누를 것이 있는가»지 글자 수가 아니다.

## 테스트가 못 잡는 것

- `.confirm-body`의 줄바꿈, `.home-join-busy`·`.stats-augs`·`.home-empty-ctas`의 실제 배치는 단위 테스트로 못 잰다(이 패키지에 jsdom이 없다). 선택자·속성 문자열로만 못을 박았다.
- `beforeunload`/`popstate`가 **실제 브라우저에서** 대화상자를 띄우는지도 마찬가지다 — 리스너 등록과 `askConfirm` 사용, 그리고 «관전자에게는 안 건다»는 조건식만 검사한다.

## 다음 사람이 알아야 할 것

- `App.tsx`에 컴포넌트 하나가 늘었다: **`GameAugmentRow`**(결과 화면 통계 탭). 그리고 모듈 전역 **`REFRESH_COOLDOWN_MS`·`refreshBusyUntil`·`markRefreshed()`** — 새로 고침을 부르는 자리를 더 만들면 `markRefreshed()`도 같이 불러야 창이 맞는다.
- `styles.css` 끝에 블록 셋이 더 붙었다: **9-6**(`.home-join-busy`) · **9-7**(빈 전적 두 문 · 표본 칩) · **9-8**(이번 판 증강). 1차 배치의 9-2-b·9-2-c·9-4·9-5 **뒤에** 있고, 그것들의 «앞선 규칙을 이겨야 한다»는 성질에는 영향이 없다.
- `GameOverModal`에 `onPracticeAgain`, `ReplayViewer`에 `shared`·`onUnshare`, `Home`에 `busyRoomCode` prop 이 새로 생겼다.
- `HumanAgent`의 `FIRST_DRAFT_TIMEOUT_MS`는 **90초 미만이어야 한다**(컨트롤러 그물). 올리려면 `HanchanController.AGENT_DECIDE_TIMEOUT_MS`를 함께 본다.
