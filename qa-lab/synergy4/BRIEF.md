# synergy4 — 증강 시너지 QA 4라운드 (2026-08-31)

목표: **비슷하게 동작하거나 서로를 키워 줄 법한 증강 2~4장**을 함께 들었을 때
1) 오류(크래시·훅 예외·불변식 위반)가 없는가
2) **설명과 구현 의도대로 서로를 실제로 키우는가** — "각각 쓸 때보다 좋아야 하는데 안 좋아지는" 경우를 찾는다
   (선례: 동수의 결속 + 양극 — 효과가 겹쳐 적용되지 않았다)

## 규칙
- 카탈로그: `qa-lab/synergy4/catalog.tsv` (id/이름/시너지축/anti/antiIds/conflicts/설명)
- 하네스: `qa-lab/harness.ts` (`runMatch`, preset으로 좌석별 증강 강제, presetHands로 배패 강제)
- 실행: `~/majak/node_modules/.bin/tsx qa-lab/synergy4/<축>/<파일>.ts` (워크트리 루트에서)
- **먼저 예측을 글로 적고**(각각 단독일 때 숫자 vs 함께일 때 숫자), 그 상황을 강제로 만들어 재고, 다르면 원인을 소스에서 확인한다.
- 단독 A, 단독 B, A+B 세 조건을 **같은 시드/같은 상황**으로 재서 비교한다. 이게 이 라운드의 핵심 방법이다.
- `conflicts`로 잠긴 조합은 제외한다(같이 들 수 없다). `anti`는 제외하지 않는다 — 확률만 낮출 뿐 같이 들 수 있다.
- **소스는 고치지 않는다.** 조사·증거·보고서만. 수정은 메인 세션이 판단한다.
- 추정 금지. 코드를 읽고 실제로 돌려서 확인한 것만 «확정»으로 적는다. 확인 못 한 건 «의심»으로 분리.

## 산출물
`qa-lab/synergy4/<축>/report.md`:
- 검사한 조합 목록(전부 나열)
- 확정 결함: 조합 / 기대(설명 근거) / 실측 / 원인 파일·줄 / 재현 스크립트 경로 / 심각도
- 의심 항목: 왜 확정 못 했는지
- 이상 없음으로 판정한 조합은 한 줄 요약으로

## 선례 (읽어 두면 좋다)
- docs/40_AUGMENT_SYNERGY_QA_2026-08-23.md — 3라운드 결론(같은 정산 단계 안 상호 무시, 뱅크 발행 이중, 순서 의존 등 결함의 «모양»)
- docs/21_AUGMENT_PAIR_AUDIT.md, docs/26_AUGMENT_SYNERGY.md
