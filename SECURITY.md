# 이능마작 보안 가이드

이 문서는 이능마작 서버(`@majak/server`)의 위협 모델과 **현재 구현된 방어**, 그리고
**앞으로 필요한 작업**을 구분해 정리한다. 배포 구조는 `DEPLOYMENT.md` 참조.

배포 형태: Cloudflare Tunnel 뒤 `HOST=127.0.0.1` 로컬 바인드, 단일 포트에서
HTTP(정적 SPA) + WebSocket. TLS(wss)는 Cloudflare 엣지가 종단한다.

---

## 0. 신뢰 경계 — 리버스 프록시 뒤에서 반드시 알아야 할 것

**모든 원격 남용 방어는 "실제 클라이언트 IP"를 키로 삼는다.** 그런데 Cloudflare
Tunnel(또는 nginx)이 `127.0.0.1:3011`로 포워딩하므로, 서버가 보는 소켓 주소
(`req.socket.remoteAddress`)는 **모든 접속자에게 127.0.0.1**이 된다.

`isLoopbackIp()`는 루프백을 원격 남용 방어에서 면제하므로(로컬 개발·테스트 편의),
프록시 뒤에서 IP 복원을 하지 않으면 아래 방어가 **전원에게 조용히 꺼진다**:
IP당 동시 연결 상한 · 메시지 토큰버킷 · 인증 무차별대입 IP 제한 · 방 생성 IP 제한.

→ 해결: `TRUST_PROXY=cloudflare`. `index.ts`의 `clientIpOf()`가 `CF-Connecting-IP`로
실제 IP를 복원한다. **스푸핑 방지**: 소켓 상대가 루프백일 때만 헤더를 신뢰하므로,
외부에서 포트에 직접 붙어 헤더를 위조해도 그 연결의 소켓 주소는 루프백이 아니라
헤더가 무시된다. (`xff` 모드도 지원 — X-Forwarded-For 마지막 홉.)

**면제는 IP 문자열이 아니라 소켓이 결정한다 (2026-08-05)**: `clientIpOf()`는 이제
`{ip, local}`을 돌려주고, `local`은 **소켓 상대가 진짜 루프백이고 프록시 헤더를 쓰지
않았을 때만** 참이다. 예전에는 복원된 IP가 `127.0.0.1`처럼 보이기만 하면 RoomManager가
그 연결을 로컬로 보고 연결 상한·토큰버킷·무차별대입 차단·방 생성 제한을 **전부 껐다** —
프록시가 헤더를 덮어쓴다는 전제가 한 번이라도 깨지면(설정 실수, `xff` 모드의 느슨한
프록시, 터널 우회) 그걸 아는 사람 하나가 모든 남용 방어 바깥에 서게 된다.

**버킷 키는 IPv6 `/64`** (`abuseKeyOf`): IPv6 사용자는 보통 `/64`를 통째로 받는다.
주소당으로 세면 주소를 갈아 가며 IP당 상한을 전부 우회하고 전역 300 슬롯도 혼자
비울 수 있어, 실제 배분 단위인 `/64`로 묶는다.

**실측 검증**: 인증 없이 WS를 열고 `{"type":"ping"}` 200개를 즉시 보낸다. `pong`이
정확히 `MSG_BUCKET_CAPACITY`(80)개면 IP 복원·토큰버킷 정상, 200개 전부면 면제 상태다.

---

## 1. 구현된 방어 (Implemented)

### 연결 계층 (`index.ts`)
- **오리진 허용목록** (`ALLOWED_ORIGINS`) — 핸드셰이크(upgrade) 단계에서 목록 밖
  Origin을 `403`으로 거부(101을 내주기 전에 차단, WS 세션 자원 미할당). CSWSH 방어.
  비브라우저 클라이언트는 Origin이 없어 통과하므로 인증 수단이 아니다 — 인증은 토큰이 담당.
- **연결 수립 속도 제한** — 버킷당 `CONN_RATE_MAX`(40회/10초). 동시 개수 상한만으로는
  **열고 즉시 닫기**를 막지 못한다(핸드셰이크·세션 객체·타이머 비용이 매번 든다).
- **하트비트** (`HEARTBEAT_INTERVAL_MS`, 기본 30s) — 주기적 ping, 다음 주기까지 pong이
  없으면 `terminate()`. 좀비 소켓이 연결 상한 슬롯을 영구 점유하는 것을 막는다.
- **프레임 크기 상한** — `maxPayload` 64KB(실사용 최대는 수 KB). 압축(`permessage-deflate`)
  비활성 — 보내는 쪽이 싸게 만드는 비대칭 해제 비용을 없앤다.
- **HTTP 계층 상한** — 동시 소켓 `MAX_HTTP_SOCKETS`(512), `headersTimeout` 10s,
  `requestTimeout` 20s, `keepAliveTimeout` 15s. WS 상한은 **업그레이드한 연결에만**
  적용되므로, 업그레이드하지 않고 헤더를 천천히 흘리는 slowloris에는 이 상한이 방어다.
- **정적 서버** — GET/HEAD만 허용(그 외 405), 퍼센트 디코드 후 정규화, NUL 차단,
  `realpath`로 심볼릭 링크 탈출까지 dist 안에 가둠, 응답 종료 시 스트림 정리.
- **정적 응답 보안 헤더** — CSP(+`form-action 'none'`), `Strict-Transport-Security`,
  `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`.

### 세션·연결 상태 (`RoomManager.ts`)
- **동시 연결 상한** — 전체 `MAX_CONNECTIONS`(300), IP당 `MAX_CONNECTIONS_PER_IP`(16).
- **메시지 레이트리밋** — 연결당 토큰버킷(용량 80, 초당 40 리필). 초과분은 조용히 드롭.
- **비정상 패킷 종료** — 기형 JSON·비객체·`type` 없음을 위반으로 계상, `MAX_PROTOCOL_VIOLATIONS`(3)회 시 `1008` 종료.
- **미인증 유예** — `UNAUTH_TIMEOUT_MS`(30s) 안에 로그인 없으면 소켓 회수. 로그아웃 시 재무장.
  단 **미인증 상태의 `logout`은 무시**한다 — 예전에는 여기서 유예가 되걸려, 29초마다
  `logout` 한 번씩 보내면 인증 없이 연결 슬롯을 무기한 점유할 수 있었다.
- **인증 게이트** — `ping`/`register`/`login`/`tokenLogin`/`logout` 외 모든 메시지는 로그인 필요(`AUTH_REQUIRED`).
- **재인증 좌석 정리** (`detachSeat`) — 이미 인증돼 방/좌석을 가진 연결이 다시 로그인하면
  이전 좌석을 분리해 대기실 유령 좌석 누수를 막는다.
- **송신 백프레셔** — `send()`가 `bufferedAmount > MAX_BUFFERED_BYTES`(4MB)면 연결을
  `terminate()`. 응답을 안 읽는 느린/악의적 소비자의 메모리 고갈 방지(예: `replayGet` 반복).

### 인증·계정 (`RoomManager.ts`, `SiteDb.ts`)
- **인증 레이트리밋** — 연결당 `AUTH_MAX_ATTEMPTS`(12/60s) + IP당 `AUTH_IP_MAX_ATTEMPTS`(30/60s,
  연결 재생성 우회 차단). `TRUST_PROXY` 설정 시에만 IP 제한이 실효.
- **인증 필드 길이 상한** (`MAX_AUTH_FIELD_LEN` 256) — 거대 문자열로 scrypt·DB 비용 부풀리기 차단.
- **비밀번호 저장** — scrypt + 사용자별 랜덤 salt(평문 저장 금지), `timingSafeEqual` 비교.
- **로그인 타이밍 균일화** — 계정이 없어도 더미 scrypt를 돌려 응답 시간으로 계정 유무를 구분 못 하게.
- **scrypt 동시성·큐 상한** — 동시 `MAX_SCRYPT_CONCURRENCY`(4) + 대기 큐 `MAX_SCRYPT_QUEUE`(64) 초과 시 거부.
- **가입 계정 열거 완화** — 회원가입도 **닉네임 중복 검사보다 scrypt를 먼저** 돌린다.
  예전에는 중복이면 즉시 반환해, 오류 문구를 읽지 않아도 응답 시간만으로 계정 존재
  여부를 훑을 수 있었다(로그인 쪽은 이미 더미 scrypt로 막혀 있었다).
- **세션 토큰** — 256비트(randomUUID + 16B), 로그인 후 비밀번호 재전송 없음, TTL(기본 30일), 사용자당 세션 수 상한.
  클라이언트는 토큰을 **발급한 서버 주소와 함께** 저장하고 그 주소에만 되돌려 보낸다
  (고급 설정의 수동 서버 주소로 토큰이 흘러나가지 않게).
- **가입 게이트** (`SIGNUP_CODE`) — 설정 시 코드를 아는 사람만 회원가입. 비교는 상수 시간(`safeEqual`).
- **관리자 코드** — 128비트. **쓰이는 즉시 회전**(유출된 값은 이미 쓰였다면 죽은 값),
  비교는 상수 시간, `ADMIN_CODE` 환경변수로 고정 시 DB·로그에 남기지 않음.
  ⚠ 매 부팅 로그에 현재 코드를 찍는다(운영자 결정, 2026-08-24) — 회전 때문에 현재 값을
  확인할 길이 로그뿐이다. 대신 **로그 파일(`.majak/server.log`)이 곧 마스터 키**이므로,
  로그를 남에게 보이거나 붙여 넣지 않는다. 그게 부담이면 `ADMIN_CODE`로 고정한다.
- **계정 삭제 시 소켓 축출** — 열린 세션 강제 종료.

### 게임 무효(중단) — 전적 세탁 방어
- **기록 대국은 사람 2명 이상일 때만 무효로 접힌다** (`abortableByHumans`). 무효는
  정산·순위·통계는 물론 **리플레이 파일까지** 지우므로, 혼자 두는 판에서 이게 열려 있으면
  지는 판마다 눌러 전적을 세탁할 수 있다.
- 검사는 **세 자리 전부**에 있다 — 표를 던질 때(`handleVoteAbort`), 정족수를 다시 셀 때
  (`retallyAbortVotes`), 사람이 다 나갔을 때(`abortIfNoHumansLeft`). 던지는 자리만 막았을
  때는 ⑴ 2인 방에서 미리 동의해 두고 **상대가 끊기기를 기다리거나** ⑵ 그냥 **나가기**를
  누르는 것으로 그대로 우회됐다(감사 2026-08-26 H-1·H-2).
- 나가기는 막지 않는다. 그 판은 봇 자동 진행으로 완주해 **기록에 남는다** — 그게 무효와
  나가기의 차이다. 나간 사람의 화면으로 그 판이 새지 않도록 좌석 쪽에서 소켓을 막는다
  (`HumanAgent`의 `muted`).
- 기록하지 않는 방(체험·연습·증강 테스트)은 예외 — 세탁할 전적이 없고, 무효가 그 방들의
  유일한 정리 수단이다.

### 방·자원 (`RoomManager.ts`)
- **방 생성 제한** — IP당 `ROOM_CREATE_MAX_PER_IP`(20/10분) + 전체 `MAX_ROOMS`(200) 상한.
- **계정당 동시 1방** — 이미 방/게임 중이면 새 방 생성 거부.
- **관전 권한** — `spectate`/`liveGames`/`adminUsers`/`adminDeleteUser`는 `isAdmin` 확인.
  본인이 참가 중인 방은 관전 불가(전원 손패·산 노출 = 완전정보 치트 방지).

### 게임 무결성 (`core`)
- 게임 진행은 서버 권위(Action→Event→Effect→GameState). 클라이언트가 보낸 `action`은
  서버가 제시한 합법 옵션과 대조해서만 수락(payload는 1회만 stringify — 반복 직렬화 DoS 방지).
- 결정론적 시드 PRNG(core에서 `Math.random` 금지) — 리플레이 재현·검증 가능.
- SQL은 전부 파라미터 바인딩(인젝션 없음).

---

## 2. 앞으로 필요한 작업 (TODO / 권장)

우선순위 순. 2026-07-21 다중 에이전트 감사 + 2026-08-05 재감사에서 도출.
(2026-08-05 감사에서 나온 항목 1·2·3·5는 이번에 **구현 완료** — §1 참조.)

~~1. 솔로 대국 무효 투표로 성적 세탁~~ → **완료** (2026-08-18 + 2026-08-27).
   선택지 ⑴ "사람 2명 이상일 때만 무효 허용"으로 정해졌다. 규칙은
   `RoomManager.abortableByHumans` 하나에 있고, 표를 **던지는** 자리·**세는** 자리·
   **사람이 다 나갔을 때** 세 곳이 그것을 쓴다. 세 자리가 필요했던 이유는
   [docs/46](docs/46_SECURITY_AUDIT_2026-08-26.md) 참조 — 처음에는 던지는 자리만
   막아서, 「상대가 끊기기를 기다렸다가 지우기」와 「나가기로 지우기」가 그대로 열려 있었다.

~~2. 비밀번호 변경·세션 일괄 로그아웃 부재~~ → **완료**. `changePassword`가 변경과
   동시에 그 계정의 세션을 전부 지우고(`DELETE FROM sessions WHERE user_id = ?`),
   `evictOtherSessions`가 이미 열려 있는 소켓까지 끊는다(DB만 지우면 붙어 있는 WS는
   살아남는다). `logoutOthers`로 비밀번호를 바꾸지 않고 다른 기기만 로그아웃할 수도 있다.

3. **관리자 권한 캐시** (LOW) — `conn.user`는 인증 시 1회 캐시되고 재검증되지 않는다.
   계정 삭제는 축출(`evictUser`)로 덮여 있지만, 권한 강등 기능이 생기면 열린 소켓에는
   반영되지 않는다. 강등을 만든다면 축출도 같이 붙일 것.
4. **서버 재시작 시 진행 중 게임 복구** (기능) — core `reconstructGame`/`resume`은 구현·테스트됨.
   서버 자동 저장·복구 배선만 남음. 현재는 재시작 시 진행 중 게임 소실(완료 리플레이만 보존).
5. **감사 로그 부재** (LOW) — 관리자 행위(계정 삭제·관전·리플레이 열람)와 인증 실패가
   기록되지 않아, 사고 후 추적이 어렵다.

---

## 3. 운영 체크리스트

- [ ] `deploy/majak.env`에 `TRUST_PROXY=cloudflare`·`HOST=127.0.0.1`·`ALLOWED_ORIGINS` 설정됨
- [ ] 부팅 로그에 `신뢰 프록시 : cloudflare` / `허용 오리진 : …` / `하트비트 …` 출력 확인
- [ ] `SIGNUP_CODE` 설정(가입 게이트 켜짐)
- [ ] 부팅 로그에 찍히는 `관리자 가입 코드`를 남이 볼 수 없는지 확인
      (`.majak/server.log` 권한·공유 여부. 숨기려면 `ADMIN_CODE` 고정)
- [ ] 과거 로그에 남은 관리자 코드는 이미 회전됐는지 확인 — 불안하면 `ADMIN_CODE`로 고정 후 재발급
- [ ] 서버가 `127.0.0.1:3011`에만 바인드(외부에 평문 포트 직접 노출 없음)
- [ ] 포트포워딩 제거됨(터널로만 접속)
- [ ] 실측: ping 200개 → pong 80개(IP 방어 작동), 악성 Origin → 403, 기형 패킷 → 1008
