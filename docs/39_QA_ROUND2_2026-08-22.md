# 39. QA 2차 전면 감사 (2026-08-22)

사용자 지시: *"전문 QA 팀을 한번 더 배치해서 … 모든 존재하는 버그를 최대한 다양한
관점에서 깊게 찾을 것. QA가 모두 끝난 뒤에는 실제로 배포해도 더이상 문제가 없을
정도로 잘 돌아가야 한다."*

증거와 재현은 `qa-lab/round2/` 에 있다. 이 문서는 **결론과 처리 결과**다.

## 팀 편성

| 담당 | 도메인 | 보고서 |
|---|---|---|
| auth | 가입·로그인·세션·비밀번호·게스트 | `qa-lab/round2/auth.md` |
| codex-text | 증강 도감 문구 117종 전수 | `qa-lab/round2/codex-text.md` |
| client | 클라이언트 코드·화면 배치·연출 | `qa-lab/round2/client.md` |
| bot | 봇의 행동방식 | `qa-lab/round2/bot.md` |
| rules | 증강을 뺀 순수 리치마작 룰 엔진 | `qa-lab/round2/rules.md` |
| aug-1·2·3·4 | 증강 117종을 4조로 나눠 전수 | `qa-lab/round2/aug-*.md` |
| synergy | 증강 연계·드래프트 | `qa-lab/round2/synergy.md` |
| server | 서버 구조·안정성·운영 | `qa-lab/round2/server.md` |
| admin | 관리자 기능 전수 + 권한 행렬 | `qa-lab/round2/admin.md` |
| spectate | 관전·중계 (대회 대비) | `qa-lab/round2/spectate.md` |
| lobby | 방·친구·전적·리플레이·제보 | `qa-lab/round2/lobby.md` |
| onboard | 튜토리얼·체험·랜딩·도움말 | `qa-lab/round2/onboard.md` |

## 방법에 대해 배운 것 (다음 라운드를 위해)

**무작위 대량 플레이만으로는 이번 확정건이 거의 안 잡혔다.** aug-1은 360판, aug-2는
180판을 돌려 crash·effectError·불변식 위반 **0**을 얻고도, 코드 대조로 확정 16건을
잡았다. 리치 중 조커, 취소 후 재리치, 숨은 리치 stealth-break, 깡↔퐁 손패 장수 —
전부 봇 페르소나가 우연히 밟기 어려운 조건 조합이다.

값이 큰 방법은 이 둘이었다:

1. **형제 증강 대조.** 확정건의 대다수가 «다른 증강에서 이미 해결해 둔 문제가 이
   증강에만 빠져 있는» 형태였다. 반복해서 나온 부류: 리치 중 손패 동결 규약 /
   「두 사람이 들면 두 번 적용」 / `handAlteredMark`(천화 게이트) 누락 / 현재 상태에서
   파생해 «부활»하는 파기 조건 / 「없는 5번째 장」 가드 누락 / 도라를 재료로 태움.
2. **독립 검산기.** rules 담당이 만든 «매 화료마다 점수를 독립적으로 재계산해 대조»가
   무작위 손 20,000개에서 표준 불일치를 정확히 집어냈다.

## 처리 결과 — 확정 100여 건을 고쳤다

커밋 순서대로:

| 커밋 | 내용 |
|---|---|
| `26f1215` | 계정 7건 — 세션 회수가 열린 소켓을 못 끊던 🔴 외 |
| `46c755a` | 봇이 자기가 만드는 후리텐을 못 보던 두 자리 |
| `3315b94` | 증강 3군 3건 — 없는 5번째 장 · 부활하는 조약 · 잠긴 론의 후리텐 |
| `3f43825` | 룰 2건 — **쿠이카에 금지 미구현** · 샹퐁 필터가 표준 손을 깎던 것 |
| `449d163` | 쿠이카에 규칙을 문서·게임 내 도움말에 반영 |
| `fee2f5c` | 클라이언트 19건(🔴 2) + 의심 11건 |
| `617050e` · `5540e88` · `71ae81c` | 드래프트 좌석 칸 폴백의 중복 보유 · 시너지↔배타 모순 |
| `2cf260c` | 리플레이 뒷도라 · 제보 제출 잠금 |
| `7cfc6a8` | 증강 1·2·4군 21건 + 도감 문구 전수 정비 |
| `ba76f9f` | 안개를 친 본인이 안개 속 바닥을 파내던 것 |
| `30ed987` | 서버·관리자·**관전** 22건 + 봇 원형 구분 |

### 배포를 막던 것들 (🔴)

- **관전 완전정보 부정행위** — 관리자가 60초 끊기면 자기 탁자를 관전한 뒤 복귀할 수
  있어, 한 소켓이 자기 뷰와 전원 손패 뷰를 함께 받았다. 대회 무결성 직결.
- **끝난 판이 되살아나** 재시작 후 다시 서고 전적이 두 번 기록됐다.
- **결과 화면에서 누른 «이 국을 물린다»가 다음 국을 물렸다.**
- **세션 회수(비밀번호 변경·다른 기기 로그아웃)가 열려 있는 소켓을 전혀 못 끊었다** —
  화면은 "끊었습니다"라고 단언했다.
- **가짜 천화 48,000점**이 여섯 곳(`full_hand_swap`·`hand_swap3`·`honor_return`·
  `regret`·`tile_dyeing`·`take_back`) + `joker`.
- `blind_ron`을 두 명이 들면 **방총한 사람이 +8000을 받았다.**
- `joker`를 리치 후에 켜 대기가 2종 → 34종이 됐다.
- `last_stand` 재리치로 **영구 리치 후리텐이 세탁**됐다.
- `full_hand_swap`으로 손패가 영구히 ±1장이 됐다.
- 연출 «건너뛰기» 알약이 폰 세로에서 손패 가운데 다섯 장을 덮었다.
- `--own-reserve`가 다른 저울(`@media`)에 걸려 **큰 글씨를 쓰는 사람만 보호를 못 받았다.**

### 규칙 자체가 없던 것

- **쿠이카에(먹고 바꾸기) 금지가 한 줄도 구현돼 있지 않았다.** 후로가 공짜 손패
  교환이 되고, 봇은 그 수를 두지 않으므로 **사람끼리 두는 대회에서만** 드러날
  어드밴티지였다. 무작위 화료손 20,000개 독립 검산으로 표준 불일치 0을 확인했다.
- 오라스 **도중유국**이 아가리야메로 취급돼 1위 오야가 구종구패로 우승을 살 수 있었다.

### 오래 «알 수 없는 플레이크»였던 것

`docs/23_TEST_BASELINE.md`의 `Tutorial.test.ts` 항목이 사라졌다. 원인은 부하도
네트워크도 아니라 `BotAgent.decide()`가 홀드를 **생각 시간 앞에서만** 검사하던 순서
문제였다 — 지연 0ms에서도 9구간 중 5개가 샜다. 지금은 0ms·60ms 양쪽에서 누출 0.

### 게이트

```
npm test                   310 파일 / 3444 테스트 전부 통과   (기준선 291/3253)
npm run typecheck          0 errors
npm run typecheck:content  0 errors
npm run typecheck:server   0 errors
npm run typecheck:client   0 errors
```

신규 회귀 테스트 약 190건. **각 수정을 하나씩 되돌려 대응 테스트가 붉어지는 것까지
실측한 담당이 셋**이고, 그 과정에서 기존 테스트 몇 건이 **거짓 통과**이거나 **버그를
못 박고 있었다**는 것이 드러나 함께 고쳤다(뷰 키 이름 오류 2건, 「노텐 벌부」라는 이름으로
양수 증감을 단언하던 1건).

## 고치지 않고 남긴 것 (판단이 필요하다)

### 1. 게임 종료 시 «남은 공탁·최종 보정»이 이벤트 로그에 없다 (🟡 · 사용자 영향 없음)

`HanchanController.calcRankings`는 남은 리치봉(01_GAME_RULES §9)과 `score.finalAdjust`
(계약 위약금·가불 상환)를 **순위표의 `rawScore`에만** 더한다. `state.players[].score`는
그대로다. 그래서 **이벤트 로그만으로 재구성한 최종 점수가 라이브와 1,000점 갈린다** —
`packages/core/test/Hanchan.test.ts`의 «리플레이 라운드트립»이 못 박은 계약이 깨지는
자리다. 그 테스트가 지금 초록인 이유는 그 시드의 마지막 국에 남은 공탁이 0이기 때문이고,
드래프트 결과가 바뀌면 곧바로 붉어진다(이번에 실제로 그렇게 드러났다 — 재현:
`p0:discard_recall p1:open_riichi p2:discard_recall p3:discard_recall`, live p1 28,000 대
replay p1 27,000).

**오늘 사용자에게 보이는 영향은 없다.** 기록·표시·통계는 전부 라이브 순위표를 쓰고,
재개(resume)는 국 중간이라 공탁이 아직 `round.riichiPot`에 남아 있다. 리플레이 뷰어도
국별 정산 패널만 그린다.

고치려면 게임 종료 시 공탁 이동을 **실제 이벤트로 내보내** 로그가 스스로 완결되게 해야
하는데, 그러면 `state.score`의 의미(=순위 보정 전 점수)가 바뀌고 `finalAdjust`도 같은
결정을 함께 받아야 한다. 마무리 단계에 종료 정산 구조를 바꾸는 것은 값보다 위험이 커서
남긴다.

### 2. `grave_rob`의 «게임당 1회»가 응답 시간 초과에도 소진된다 (🟡)

고치려면 코어 `safeDecide`를 건드려야 하고, 그 방향이 **응답 없는 사람을 자동 화료**
시키는 쪽이라 폴드 의도와 반대가 된다. 설계 결정이 필요하다 (`qa-lab/round2/aug-2-FIXED.md` 의심 1).

### 3. 봇 아키타입의 «성격 차이» (별도 작업으로 진행)

`docs/00_MASTER_ARCHITECTURE.md`가 약속한 «원형끼리 뚜렷이 다르다»가 실측 90.7% 일치였다.
밸런스 변경이라 아레나 2:2 측정을 조건으로 별도 담당에게 맡겼다.
